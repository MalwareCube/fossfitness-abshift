import React from 'react';

//Styled Components
import { StyledProgressBar } from './ProgressBar.styled';

const ProgressBar = () => {
  return (
  <StyledProgressBar />
  )
};

export default ProgressBar;
